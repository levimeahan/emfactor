import {
    userLoggedIn,
    userIsManager
} from './user';

import {
    currentScheduleWeek
} from './schedule';

import {
    employeesArray,
    availableEmployees,
    employeeIsAvailable
} from './employees';

import {
    rolesArray,
    roleNames,
    roleMatches
} from './roles';

export {
    userLoggedIn,
    userIsManager,
    currentScheduleWeek,

    employeesArray,
    availableEmployees,
    employeeIsAvailable,

    rolesArray,
    roleNames,
    roleMatches,
}